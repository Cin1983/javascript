import React from "react";
const hour = 12;

if (hour >= 6 && hour <= 12) console.log("Good Morning");
else if (hour >= 12 && hour <= 18) console.log("Good afternoon");
else console.log("Have an awesome evening!!!");

const Test = () => {
  return <div></div>;
};

export default Test;
